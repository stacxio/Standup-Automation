# Slack
slack_sdk
slack_bolt          # /leave Socket Mode form app
websocket-client    # stable Socket Mode transport

# Google Sheets / Drive
gspread
google-auth
google-api-python-client   # Drive API (uploads)
google-auth-oauthlib       # OAuth user-credential Drive uploads

# PDF generation (salary slips / payslips)
reportlab

# Reasoning engine clients (install what your backend needs)
anthropic            # hosted: Anthropic Messages API
openai               # local/hosted via OpenAI-compatible endpoint (Ollama/vLLM/DashScope)

# Config / validation
pydantic
python-dotenv

# IANA timezone database (Windows has no system tz data for zoneinfo)
tzdata

# Dev / test
pytest
