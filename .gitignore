# Secrets — never commit (NFR-5)
.env
credentials/*.json

# Logs
logs/*.log

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/
build/
dist/

# Generated outputs (contain team attendance/salary data — never commit)
Attendance/
Salary_Slips/
Result/*.csv
Result/*.xlsx

# Intermediate image assets (the committed logo is assets/stacx_banner.jpg)
assets/*.png
